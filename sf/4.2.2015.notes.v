(*
* Set Theory              Dependent Function Spaces
* Functional programming  (forall (x:A), B[x])
* Logic                   Inductively-defined sets
*                         (Inductive _ :=
  *                         | = 
      *                     | = )
* fun x=> .. .. or fix f x =>
* f(a)
* constructors (e.g. O, S, nil, cons)
* match _ with
* | => ...
* | => ...
* *)
